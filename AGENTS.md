# AGENTS.md

## 프로젝트 목표

이 프로젝트의 목표는 빠른 프로토타입이 아니라 상용화 가능한 품질의 프로그램을 만드는 것이다.

모든 변경은 다음 품질 속성을 고려해야 한다.

- 안정성
- 데이터 무결성
- 보안
- 성능
- 확장성
- 유지보수성
- 모듈성
- 테스트 가능성
- 사용자 경험
- 운영 가능성

## 기본 작업 원칙

- 요청받은 범위 안에서만 변경한다.
- 관련 없는 리팩토링을 하지 않는다.
- 새 의존성은 명시적 승인 없이 추가하지 않는다.
- 기존 코드 스타일과 폴더 구조를 따른다.
- 불확실한 요구사항은 구현 전에 가정으로 명시한다.
- 복잡한 작업은 작은 단계로 나누고, 각 단계마다 검증한다.
- 검증하지 않은 내용을 성공했다고 말하지 않는다.
- 단일 사용 코드에는 새 추상화를 만들지 않는다.
- 내 변경으로 생긴 unused import, 변수, 함수만 정리한다.
- 사용자의 기존 변경을 되돌리지 않는다.

## 설계 우선순위

우선순위는 다음과 같다.

1. 정확성
2. 데이터 안전성
3. 보안
4. 단순성
5. 유지보수성
6. 성능
7. 확장성

성능 최적화가 단순성과 정확성을 해치면 근거를 제시하고 사용자 승인을 받아야 한다.

## 데이터베이스 원칙

DB는 데이터 무결성의 마지막 방어선이다.

- 가능한 한 DB 제약조건으로 무결성을 보장한다.
- 중요한 필드는 NOT NULL을 우선 고려한다.
- 중복되면 안 되는 값은 UNIQUE 제약을 사용한다.
- 관계 무결성은 FOREIGN KEY로 보장한다.
- 상태값은 CHECK 제약 또는 별도 참조 테이블로 관리한다.
- 인덱스는 실제 조회 패턴을 근거로 추가한다.
- 마이그레이션은 기존 데이터, 락, 롤백 가능성을 고려한다.
- 운영 데이터 삭제는 soft delete 또는 명시적 승인 기반으로 처리한다.
- 대량 변경, 스키마 변경, destructive migration은 반드시 별도 계획을 먼저 제시한다.

## API 원칙

- API는 contract-first 방식으로 설계한다.
- 요청과 응답 스키마를 명확히 정의한다.
- 입력값은 서버에서 검증한다.
- 인증(authentication)과 권한(authorization)을 구분한다.
- 사용자가 소유하지 않은 리소스에 접근할 수 없도록 서버에서 검증한다.
- 에러 응답 형식은 일관되게 유지한다.
- 목록 API는 pagination을 기본으로 한다.
- 재시도 가능한 쓰기 작업은 idempotency를 고려한다.
- 외부 API 호출은 timeout, retry, fallback을 고려한다.
- 민감 정보는 로그에 남기지 않는다.

## 백엔드 코드 원칙

- controller/router에는 얇은 요청 처리만 둔다.
- 비즈니스 규칙은 service 또는 domain 계층에 둔다.
- DB 접근은 repository/query 계층으로 분리한다.
- 도메인 규칙은 테스트 가능해야 한다.
- 하나의 함수는 하나의 책임을 갖도록 유지한다.
- 순환 의존성을 만들지 않는다.
- 에러 처리는 기존 프로젝트 패턴을 따른다.

## 프론트엔드 원칙

- UI는 디자인 시스템을 우선 따른다.
- 재사용 가능한 컴포넌트와 페이지 전용 컴포넌트를 구분한다.
- loading, error, empty, success 상태를 고려한다.
- 접근성을 고려한다.
- 키보드 조작, focus 상태, aria 속성을 필요한 곳에 반영한다.
- 서버 상태와 클라이언트 UI 상태를 구분한다.
- 폼 검증은 클라이언트와 서버 양쪽에서 처리한다.
- 성능 최적화는 측정 가능한 병목이 있을 때 적용한다.

## 테스트 원칙

기능 변경 시 다음을 고려한다.

- 핵심 비즈니스 규칙은 unit test로 보호한다.
- DB 연동이 중요한 기능은 integration test를 작성한다.
- 주요 사용자 흐름은 e2e test를 고려한다.
- 버그 수정은 가능하면 실패 테스트를 먼저 만든다.
- 테스트가 불가능한 구조라면 구조 개선 이유를 설명한다.

## 보안 원칙

- OWASP API Security Top 10을 기준으로 API 보안 위험을 점검한다.
- 권한 검증은 클라이언트가 아니라 서버에서 수행한다.
- 민감 정보, 토큰, 비밀번호, API 키를 코드나 로그에 남기지 않는다.
- 사용자 입력은 신뢰하지 않는다.
- 파일 업로드, URL 입력, webhook, redirect는 SSRF와 악성 입력 가능성을 고려한다.
- 의존성 추가 전 보안과 유지보수 상태를 확인한다.

## 운영 원칙

- 설정값은 코드에 하드코딩하지 않는다.
- 환경별 설정은 환경 변수나 secret manager를 사용한다.
- 로그는 문제 추적에 필요한 맥락을 포함하되 민감 정보는 제외한다.
- 중요한 작업에는 correlation id 또는 request id를 고려한다.
- 배포 전 migration, rollback, smoke test 계획을 확인한다.
- 장애 대응에 필요한 runbook을 갱신한다.

## 프로젝트 문서 관리 원칙

- 작업 지침은 이 파일에 유지한다.
- 프로젝트 구조, 메뉴 연결성, DB 연결성, 업무 규칙, 기능 구현 내용은 `docs/project-operations.md`와 관련 상세 문서에 누적한다.
- DB 구조가 바뀌면 `docs/database-architecture.md`와 `docs/database-schema.svg`를 함께 검토한다.
- 기능/룰/상태 흐름이 바뀌면 관련 운영구조 문서를 함께 갱신한다.
- 운영구조가 바뀌지 않는 단순 버그 수정에서는 문서를 억지로 수정하지 않는다.

## 완료 보고

작업 완료 시 반드시 다음을 보고한다.

1. 변경한 파일
2. 변경 이유
3. 실행한 테스트와 결과
4. 실행하지 못한 검증과 이유
5. 남은 위험 요소
6. 수동 확인이 필요한 부분
