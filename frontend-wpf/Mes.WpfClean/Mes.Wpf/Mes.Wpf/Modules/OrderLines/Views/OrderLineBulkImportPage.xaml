<UserControl x:Class="Mes.Wpf.Modules.OrderLines.Views.OrderLineBulkImportPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Padding="12" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           Text="업로드 파일" />

                <TextBox Grid.Column="1"
                         Margin="8,0,8,0"
                         IsReadOnly="True"
                         Text="{Binding SelectedFilePath}" />

                <Button Grid.Column="2"
                        Width="90"
                        Margin="0,0,8,0"
                        Content="파일선택"
                        Command="{Binding SelectFileCommand}" />

                <Button Grid.Column="3"
                        Width="90"
                        Margin="0,0,8,0"
                        Content="검증"
                        Command="{Binding ValidateCommand}" />

                <Button Grid.Column="4"
                        Width="90"
                        Margin="0,0,8,0"
                        Content="등록"
                        Command="{Binding CommitCommand}" />

                <Button Grid.Column="5"
                        Width="90"
                        Content="초기화"
                        Command="{Binding ResetCommand}" />

                <Button Grid.Column="6"
                        Content="선택 행 삭제"
                        Command="{Binding RemoveSelectedRowCommand}"
                        Padding="12,6"
                        Margin="8,0,0,0" />

                <Button Grid.Column="7"
                        Content="오류 행 전체 삭제"
                        Command="{Binding RemoveErrorRowsCommand}"
                        Padding="12,6"
                        Margin="8,0,0,0" /> 
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Margin="0,12,0,12"
                Padding="12"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="4">
            <UniformGrid Rows="1" Columns="4">
                <StackPanel>
                    <TextBlock Text="총 행수" FontWeight="Bold" />
                    <TextBlock Text="{Binding TotalRowCount}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="정상" FontWeight="Bold" />
                    <TextBlock Text="{Binding ReadyRowCount}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="검토필요" FontWeight="Bold" />
                    <TextBlock Text="{Binding ReviewRowCount}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="오류" FontWeight="Bold" />
                    <TextBlock Text="{Binding ErrorRowCount}" />
                </StackPanel>
            </UniformGrid>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FlatRows}"
                  SelectedItem="{Binding SelectedValidationRow, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="행번호" Binding="{Binding RowNumber}" Width="70" />
                <DataGridTextColumn Header="ERP주문번호" Binding="{Binding ErpOrderNo}" Width="140" />
                <DataGridTextColumn Header="품목코드" Binding="{Binding ProductCode}" Width="120" />
                <DataGridTextColumn Header="ERP 품목명[규격]" Binding="{Binding ErpProductDisplayName}" Width="220" />
                <DataGridTextColumn Header="MES 품목명" Binding="{Binding MesProductDisplayName}" Width="220" />
                <DataGridTextColumn Header="수량" Binding="{Binding OrderQty}" Width="90" />
                <DataGridTextColumn Header="상태" Binding="{Binding Status}" Width="90" />
                <DataGridTemplateColumn Header="품목명변경" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding ApplyProductNameChange, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      IsEnabled="{Binding CanApplyProductNameChange}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="메시지" Binding="{Binding MessageSummary}" Width="300" />
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.RowSpan="3"
      Background="#80000000"
      Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
      Panel.ZIndex="999">
            <Border Width="220"
            Height="120"
            Background="White"
            CornerRadius="8"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
                <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                    <ProgressBar Width="160"
                         Height="18"
                         IsIndeterminate="True" />
                    <TextBlock Margin="0,12,0,0"
                       HorizontalAlignment="Center"
                       Text="처리 중입니다..." />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>